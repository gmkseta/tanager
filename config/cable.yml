development:
  adapter: async
  url: redis://#{ENV["REDIS_HOST"]}:#{ENV["REDIS_PORT"]}/0
  channel_prefix: swallow_development

test:
  adapter: async

production:
  adapter: redis
  url: redis://#{ENV["REDIS_HOST"]}:#{ENV["REDIS_PORT"]}/0
  channel_prefix: swallow_production